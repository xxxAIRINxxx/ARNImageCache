source 'https://github.com/CocoaPods/Specs.git'

target 'ARNImageCache', :exclusive => true do
  pod "ARNImageCache", :path => "../"
end

target 'Tests', :exclusive => true do
  pod "ARNImageCache", :path => "../"

  pod 'Specta', '~> 0.2.1'
  pod 'Expecta'
  pod 'FBSnapshotTestCase'
  pod 'Expecta+Snapshots'
end
